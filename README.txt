Setup Instructions:

**To ensure the program runs follow the steps below:

- Open your terminal and navigate to the project directory
- Install the required dependencies by running: "pip install -r requirements.txt"
            This step is necessary because the project relies on external libraries specified in the requirements.txt file
- Once the dependencies are installed, you can execute the main script without issues